% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join_smq.R
\name{join_smq}
\alias{join_smq}
\title{Expand Standardised MedDRA Queries (SMQs) into per-LLT membership rows}
\usage{
join_smq(data)
}
\arguments{
\item{data}{MedDRA source data from \code{\link[=read_meddra]{read_meddra()}}. Must include the
\code{smq_list.asc} and \code{smq_content.asc} elements, as well as the hierarchy
files required by \code{\link[=join_meddra]{join_meddra()}}.}
}
\value{
A data.frame with one row per (SMQ x expanded LLT x SOC path) and the
columns \code{smq_code}, \code{smq_name}, \code{smq_level}, \code{term_scope}, \code{term_category},
\code{term_weight}, \code{term_status}, \code{soc_code}, \code{soc_name}, \code{soc_abbrev},
\code{hlgt_code}, \code{hlgt_name}, \code{hlt_code}, \code{hlt_name}, \code{pt_code}, \code{pt_name},
\code{pt_soc_code}, \code{llt_code}, \code{llt_name}, \code{llt_currency}, and \code{primary_soc_fg}.
\code{term_scope} is \code{1} (broad) or \code{2} (narrow); for Preferred Term members the
scope is inherited by all of the Preferred Term's Lowest Level Terms.
}
\description{
Flattens each SMQ in \code{smq_list.asc} against \code{smq_content.asc} and attaches the
full MedDRA hierarchy, producing the dictionary term list needed to flag
adverse events (which are coded at the Lowest Level Term level) and to build
SMQ\* or CQ\* variables. Specifically it:
}
\details{
\itemize{
\item resolves sub-SMQ references (term level 0) recursively, so a hierarchical
SMQ is expanded through all of its subordinate SMQs;
\item expands Preferred Term members (term level 4) down to every Lowest Level
Term beneath the Preferred Term; and
\item attaches Lowest Level Term members (term level 5) directly.
}

The hierarchy on each row comes from \code{\link[=join_meddra]{join_meddra()}}, so a Preferred Term that
maps to more than one System Organ Class contributes one row per SOC path
(filter on \code{primary_soc_fg == "Y"} for a single SOC per PT). Every SMQ in
\code{smq_list.asc} is expanded, so a subordinate SMQ can also be selected on its
own via its \code{smq_code}.
}
\examples{
# The package bundles a small fictional MedDRA dataset for illustration.
# For real work, replace this with the path to your licensed MedDRA release.
example_dir <- system.file("example_meddra", package = "meddra.read")
meddra_raw <- read_meddra(example_dir)

# Expand every SMQ into one row per Lowest Level Term, with the full
# SOC -> HLGT -> HLT -> PT -> LLT hierarchy attached to each row.
smq_df <- join_smq(meddra_raw)
smq_df

# Typical follow-on: keep only broad-scope, current LLTs for merging.
subset(smq_df, term_scope == 1 & llt_currency == "Y")
}
\seealso{
\code{\link[=join_meddra]{join_meddra()}} for the underlying hierarchy flattening.
}
